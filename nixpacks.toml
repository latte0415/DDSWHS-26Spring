# Railway(Nixpacks): sqlite3를 prebuild 대신 컨테이너 glibc에 맞게 소스 빌드
[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm ci",
  "npm rebuild sqlite3 --build-from-source",
]

[start]
cmd = "npm start"
